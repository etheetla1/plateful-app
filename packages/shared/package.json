{
  "name": "@plateful/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "vercel-build": "echo 'Skipped - handled by root buildCommand'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/uuid": "^11.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "turbo": "^1.13.4"
  }
}
